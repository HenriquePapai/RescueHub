apiVersion: v1
kind: Secret
metadata:
  name: rescuehub-secret
  namespace: rescuehub
  labels:
    app.kubernetes.io/name: rescuehub-secret
    app.kubernetes.io/part-of: rescuehub
type: Opaque
stringData:
  # Valores apenas para ambiente local/de faculdade. Troque antes de qualquer uso real.
  DJANGO_SECRET_KEY: "troque-esta-chave-local-rescuehub"
  GATEWAY_SECRET_KEY: "troque-esta-chave-local-rescuehub-gateway"
  DATABASE_PASSWORD: "rescuehub"
